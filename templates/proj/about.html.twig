{# templates/proj/about.html.twig #}

{% extends('proj/base.html.twig') %}

{% block title %}{{ title }}{% endblock %}

{% block content %}
    <div class="main-title">
        <h1>{{ title }}</h1>
    </div>

    <div class="main-content">
        {% if img is defined %}
            <h2>ER-diagram</h2>
            <img class="no-margin-inline" src="{{ asset(img) }}" alt="Bild">
        {% endif %}
        {{ doc | raw }}
    </div>
</div>
{% endblock %}
